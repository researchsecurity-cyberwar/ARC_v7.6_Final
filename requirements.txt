# ============================================================
# ARC v7.6 Final - Python Dependencies
# ============================================================
# NOTE: CLI tools (nuclei, amass, httpx, gau, dalfox, ffuf)
# are NOT pip packages. Install them separately on Kali Linux:
#   sudo apt install nuclei amass sqlmap
#   go install github.com/projectdiscovery/httpx/cmd/httpx@latest
#   go install github.com/lc/gau@latest
#   go install github.com/projectdiscovery/dalfox/v2/cmd/dalfox@latest
#   go install github.com/ffuf/ffuf/v2@latest
# ============================================================

# Core Dependencies
requests>=2.31.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
PyYAML>=6.0.1
stem>=1.8.2
networkx>=3.2.1
cryptography>=41.0.7
pycryptodome>=3.19.0
rich>=13.7.0
watchdog>=3.0.0
PyJWT>=2.8.0
boto3>=1.34.0
websockets>=12.0
psutil>=5.9.5
colorama>=0.4.6
python-telegram-bot==20.7

# Browser Automation
playwright>=1.40.0

# Document Parsing - PDF/DOCX/XLSX (SHADOW_INTELLIGENCE_RADAR VDP scrapers, report gen)
pdfplumber>=0.10.0
PyPDF2>=3.0.0
python-docx>=1.1.0
openpyxl>=3.1.0

# AI & LLM Support (optional - graceful fallback if not installed)
llama-cpp-python>=0.2.48
numpy>=1.24.3

# Machine Learning - UNIFIED_LEARNING_ENGINE (recommended for self-learning)
scikit-learn>=1.3.0
xgboost>=1.7.0
pandas>=2.0.0

# Web3 & DeFi Analysis (optional - graceful fallback if not installed)
web3>=6.12.0
slither-analyzer>=0.9.0

# CTF & Binary Analysis (optional - graceful fallback if not installed)
pwntools>=4.9.0
capstone>=5.0.1

# Forensics & Network Analysis (optional - graceful fallback if not installed)
scapy>=2.4.5

# Cloud & VPS Deployment (optional - INFRASTRUCTURE/standard_vps_deployer.py)
azure-identity>=1.14.0
azure-mgmt-compute>=30.6.0
azure-mgmt-network>=25.4.0
azure-mgmt-resource>=23.4.0
google-auth>=2.23.0
google-cloud-compute>=1.14.0
hcloud>=1.25.0
vultr>=1.0.1
linode-api4>=5.0.0

# NOTE: clusterfuzz-lite / clusterfuzzlite TIDAK ada di PyPI.
# Jika BROWSER_SECURITY_RESEARCH fuzzing dibutuhkan, install dari GitHub:
#   python3 -m pip install git+https://github.com/google/clusterfuzzlite.git#subdirectory=projects/tradefuzz

# Development & Testing
pytest>=7.4.0
black>=23.7.0
flake8>=6.0.0
